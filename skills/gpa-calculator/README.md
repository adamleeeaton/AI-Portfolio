# GPA Calculator Skill

This package is designed for a hosted agent. Install or copy the entire `gpa-calculator` directory so `SKILL.md` can load the supporting files in `references/`.

Before use, customize the bracketed fields in `SKILL.md` and the reference files for the institution's current grade scale, repeat policy, rounding rules, and approved academic-record sources.
